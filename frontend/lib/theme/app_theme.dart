import 'package:flutter/material.dart';

class AppTheme {
  static const LinearGradient mainGradient = LinearGradient(
    colors: [Color(0xFF00EAFF), Color(0xFF8A00F3)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}